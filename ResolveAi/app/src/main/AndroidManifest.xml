<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaymo.resolveai">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="resolve_ai.db" />
        <meta-data
            android:name="VERSION"
            android:value="15" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.kaymo.resolveai" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetalhesActivity"
            android:parentActivityName=".ReclamacoesActivity" />
        <activity
            android:name=".ReclamacaoActivity" />
        <activity android:name=".ReclamacoesActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".NovoUsuarioActivity"
            android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".ResolvidosActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ReclamacoesActivity2"
            android:label="@string/title_activity_reclamacoes2"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>